<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_speech_to_text"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.crazygeeksblog.androidspeechtotext.SpeechToTextActivity">

    <ImageButton
        android:id="@+id/btnVoiceInput"
        android:layout_width="match_parent"
        android:layout_centerInParent="true"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:src="@android:drawable/ic_btn_speak_now" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/btnVoiceInput"
        android:layout_margin="10dp"
        android:textAlignment="center"
        android:text="Tab Mic To Speak" />

    <TextView
        android:id="@+id/tvConvertedText"
        android:layout_width="match_parent"
        android:layout_below="@id/btnVoiceInput"
        android:textAlignment="center"
        android:layout_margin="10dp"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.AppCompat.Display1" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:autoLink="web"
        android:gravity="center"
        android:text="www.crazygeeksblog.com"
        android:textSize="26sp"
        android:textStyle="bold"
        android:visibility="visible" />
</RelativeLayout>
